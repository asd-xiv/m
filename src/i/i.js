/**
 * Identity function
 *
 * @name i
 * @alias identity
 * @tag Core
 * @signature <T>(input: T): T
 *
 * @example
 * identity(1)
 * // => 1
 */
export const i = input => input

export { i as identity }
